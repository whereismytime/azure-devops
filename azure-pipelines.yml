# Azure DevOps YAML pipeline using self-hosted agent from 'MyHostedPool'

trigger:
  - main  # 🔁 Запуск при пуше в ветку main

pool:
  name: MyHostedPool    # 🏊‍♂️ Название пула агентов
  demands:
    - Agent.Name -equals ANDY  # 🎯 Явно указываем на агента ANDY

steps:
  - script: echo Hello from ANDY agent!
    displayName: '👋 Greeting step'

  - script: |
      echo 🔧 Building project...
      dotnet build YourProject.sln --configuration Release
    displayName: '🔨 Build .NET project'

  - script: |
      echo 🧪 Running tests...
      dotnet test YourProject.sln --no-build
    displayName: '✅ Run unit tests'
