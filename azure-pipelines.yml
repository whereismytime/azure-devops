# ✅ Starter pipeline for Azure DevOps using Microsoft-hosted agent
# Docs: https://aka.ms/yaml

trigger:
  - main  # 🔁 Pipeline будет запускаться при пуше в ветку main

pool:
  vmImage: 'windows-latest'  # 🖥 Используется готовый агент от Microsoft

steps:
  - script: echo Hello, world!
    displayName: 'Run a one-line script'

  - script: |
      echo Running build steps...
      dotnet build YourSolution.sln --configuration Release
    displayName: 'Build .NET project'

  - script: |
      echo Running tests...
      dotnet test YourSolution.sln
    displayName: 'Run unit tests'

  - script: |
      echo Deployment step (example)
    displayName: 'Deployment (placeholder)'
